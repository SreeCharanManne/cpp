vector<int> rotLeft(vector<int> a, int d) {vector<int> b;
    int k=d%a.size();
    for(int i=k;i<a.size();i++)
    {b.push_back(a[i]);}
    for(int i=0;i<k;i++)
    {
        b.push_back(a[i]);
    }
return b;
}
